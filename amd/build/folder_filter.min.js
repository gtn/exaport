/**
 * AMD module for search and sort controls in the folder layout view.
 *
 * - Text search input (#exaport-folder-search): hides/shows category and item tiles
 *   in real time. The pinned "back to parent" tile (data-pinned="true") is always kept
 *   visible regardless of the search term.
 * - Sort dropdown (#exaport-folder-sort-select): reloads the page with the chosen sort
 *   URL parameter so items are re-sorted server-side (same as the column-heading links).
 *
 * @module     block_exaport/folder_filter
 * @copyright  2024 gtn gmbh
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("block_exaport/folder_filter",["core/ajax"],(function(Ajax){function applySearchFilter(searchInput){if(searchInput){var searchText=searchInput.value.toLowerCase();document.querySelectorAll(".exaport-flat-item[data-item-name]").forEach((function(tile){var name=tile.getAttribute("data-item-name")||"";tile.style.display=searchText&&-1===name.indexOf(searchText)?"none":""})),document.querySelectorAll(".exaport-folder-category[data-item-name]").forEach((function(tile){if("true"!==tile.getAttribute("data-pinned")){var name=tile.getAttribute("data-item-name")||"";tile.style.display=searchText&&-1===name.indexOf(searchText)?"none":""}else tile.style.display=""}))}}return{init:function(){var searchInput=document.getElementById("exaport-folder-search"),sortSelect=document.getElementById("exaport-folder-sort-select"),restoredFromSession=function(searchInput,sortSelect){var state,saved=sessionStorage.getItem("exaport_folder_filters");if(!saved)return!1;sessionStorage.removeItem("exaport_folder_filters");try{state=JSON.parse(saved)}catch(e){return!1}var restored=!1;return state.search&&searchInput&&(searchInput.value=state.search,restored=!0),state.sort&&sortSelect&&(sortSelect.value=state.sort,restored=!0),restored}(searchInput,sortSelect);searchInput&&searchInput.addEventListener("input",(function(){applySearchFilter(searchInput)})),restoredFromSession&&applySearchFilter(searchInput),sortSelect&&sortSelect.addEventListener("change",(function(){var name,value;name="sort",value=sortSelect.value,Ajax.call([{methodname:"core_user_set_user_preferences",args:{preferences:[{name:"block_exaport_"+name,value:String(value)}]}}])[0];var url=new URL(window.location.href);url.searchParams.set("sort",sortSelect.value),window.location.href=url.toString()}))}}}));

//# sourceMappingURL=folder_filter.min.js.map